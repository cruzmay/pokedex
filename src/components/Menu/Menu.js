import React from 'react'
import { SearchPokemon } from '../SearchPokemon/SearchPokemon'
export const Menu = () => {
    return (
        <header className="header">
            <SearchPokemon/>
        </header>
    )
}
